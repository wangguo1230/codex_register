#!/usr/bin/env bash
# 启动前端 Vite 开发服务器（热更新）
set -e
cd "$(dirname "$0")/web"

echo "============================================================"
echo "  前端开发服务器启动中（Vite 热更新）"
echo "  (Ctrl+C 退出)"
echo "============================================================"
exec npx vite
